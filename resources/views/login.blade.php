<x-layout page="Todo: Login">
    <x-slot:btn>
        <a href="{{ route('register') }}" class="btn btn-primary">
            Registre-se
        </a>

    </x-slot:btn>

    tela de login
    <a href="{{ route('home') }}">Ir para home</a>
</x-layout>
